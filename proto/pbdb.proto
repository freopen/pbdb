syntax = "proto3";

import "google/protobuf/descriptor.proto";

package pbdb;

extend google.protobuf.MessageOptions { optional bool single_record = 50000; }

enum IdTypes {
  NOT_USED = 0;
  DEFAULT = 1;
}

extend google.protobuf.FieldOptions {
  optional IdTypes id = 50000;
}
